version: '3'
services:
  database:
    build:
      context: ./Database
      dockerfile: dockerfile
    networks:
      - backend

  app:
    build:
      context: ./Backend
      dockerfile: dockerfile
    networks:
      - frontend
      - backend

  proxy:
    build:
      context: ./Proxy
      dockerfile: dockerfile
    ports:
      - "80:80"
    networks:
      - frontend

networks:
  frontend:
    driver: bridge
  backend:
    driver: bridge
